import { SubmissionAnimation } from './submission-animation';

describe('SubmissionAnimation', () => {
  it('should create an instance', () => {
    expect(new SubmissionAnimation()).toBeTruthy();
  });
});
