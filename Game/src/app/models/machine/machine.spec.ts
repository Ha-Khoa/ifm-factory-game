
// Importiere die Machine-Klasse
import { Machine } from './machine';

/*
// Teste die Machine-Klasse
describe('Machine', () => {
  it('should create an instance', () => {
    // Erstelle eine neue Machine mit Beispielkoordinaten
    expect(new Machine(1, 2, "machine", "north", {id: 1, name: "product", quantity: 1}, [{name: "resource", quantity: 5}])).toBeTruthy();
  });
});
*/