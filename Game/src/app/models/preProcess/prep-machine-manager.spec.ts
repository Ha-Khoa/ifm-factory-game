import { PrepMachineManager } from './prep-machine-manager';

describe('PrepMachineManager', () => {
  it('should create an instance', () => {
    expect(new PrepMachineManager()).toBeTruthy();
  });
});
