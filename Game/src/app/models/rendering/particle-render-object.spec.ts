import { ParticleRenderObject } from './particle-render-object';

describe('ParticleRenderObject', () => {
  it('should create an instance', () => {
    expect(new ParticleRenderObject()).toBeTruthy();
  });
});
