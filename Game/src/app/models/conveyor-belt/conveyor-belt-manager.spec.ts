import { ConveyorBeltManager } from './conveyor-belt-manager';

describe('ConveyorBeltManager', () => {
  it('should create an instance', () => {
    expect(new ConveyorBeltManager()).toBeTruthy();
  });
});
